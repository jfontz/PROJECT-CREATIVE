# Expand Hover Effect

This project creates an "expand hover effect" for a list of items. The effect is achieved by using CSS pseudo-elements and transitions. When the user hovers over an item, a box is revealed and expands from the middle of the text content.

## Technologies Used

- HTML
- CSS